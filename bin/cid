#!/usr/bin/env python

import sys
import os

if sys.version_info < (2,7):
    print( 'Sorry, but only Python version >= 2.7 is supported!' )
    sys.exit( 1 )

sys.path.append( os.path.join( os.path.dirname( __file__ ), '..' ) )

import cid.cli as cli

cli.run()
